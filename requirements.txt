# Core dependencies for ReCoN
numpy>=2.1.0
pillow==10.4.0
pyyaml==6.0.2
networkx==3.3
streamlit==1.36.0
matplotlib>=3.9.0
scipy>=1.13.0

# Audio processing (optional - for audio phrase recognition demo)
# Comment out if audio processing is not needed
librosa>=0.10.0
soundfile>=0.12.0
numba>=0.56.0

# Visualization (optional - requires system dependencies like pangocairo)
# Comment out if system dependencies are not available
# manim==0.19.0  # Removed for Streamlit deployment compatibility
